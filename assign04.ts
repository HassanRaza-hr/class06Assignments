/* Write a program that defines a function to calculate the area of a circle. The function should take the radius as input
 and return the calculated area.*/
 // formula of area of the circle=3.14159*r**2
 function inputRadius(radius:number){
var result=Math.PI*radius**2;
console.log(result)
 }
inputRadius(3)